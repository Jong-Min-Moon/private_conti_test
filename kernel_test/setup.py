from setuptools import setup, find_packages

setup(
    name='private_me',
    version='0.1.0',
    install_requires= find_packages(),
    description='Code for private mean embedding test',
    license='MIT'
)
